import { createClient } from " @supabase/supabase-js\; import dotenv from \dotenv\; dotenv.config(); const supabase = createClient(process.env.VITE_SUPABASE_URL, process.env.VITE_SUPABASE_ANON_KEY); async function run() { const { data, error } = await supabase.from(\projects\).insert([{ clinic_name: \Test\, status: \ÉäÅ[Éh\, phase: \sales\, sales_rep: \Test\, ball_holder: \Test\ }]); console.log(error); } run();
